/*=========================================================

=========================================================*/
void DirItem::initDirItem(void){
  type=0;
}
/*=========================================================

=========================================================*/
DirItem::DirItem(){
  initDirItem();
}
DirItem::~DirItem(){
}
/*=========================================================

=========================================================*/
DirItem &DirItem::instance(void){
  return *new(cMemAlloc(sizeof(DirItem)))DirItem();
}
/*=========================================================

=========================================================*/
String DirItem::getAbsolutePath(void){
  return path.copy().append('/').append(name);
}
